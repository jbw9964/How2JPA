
# 7. JPQL 고급

---

1. 경로 표현과 그 특징
2. 명시적, 암묵적 join
3. fetch join
4. fetch join VS 일반 join
5. jpql 다형성 쿼리
6. Named 쿼리와 벌크 연산

---

- jpql dml 과 엔티티 별칭
- 경로 표현식과 jpql 의 타입 표현
- 결과 조회 (+ 페이징) api 와 파라미터 바인딩
- 프로젝션
- 기타 : case 식, 기본 함수, 집합과 정렬

---

- join & join on
- 서브쿼리
- 명시적, 암묵적 join
- fetch join
- fetch join vs join
- fetch join 의 특징과 한계
- 다형성 쿼리 : type, treat
- named query
- 벌크 연산
- JDB 직접 사용, SpringJdbcTemplate : 영속 flush

